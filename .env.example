# v7.5-main Environment Variables

# Server Port (Railway sets this automatically)
PORT=8504

# CORS Configuration
# Comma-separated list of allowed origins, or "*" for all
ALLOWED_ORIGINS=*

# Example for production:
# ALLOWED_ORIGINS=https://deckster.xyz,https://www.deckster.xyz
